pub mod color_enum {

    #[derive(Debug,Hash,Clone,PartialEq, Eq)]
    pub enum Color {
        Red,
        Blue,
        Green,
        Yellow,
        Black,
        White,
        Orange,
        Pink,
        Purple,
        Gray
    }
}
